import React  from 'react';

interface CustomVisualProps {
}

function CustomVisual({} : CustomVisualProps) {

  return (
    <div>
      CustomVisual Component
    </div>
  );
}

export default CustomVisual;

